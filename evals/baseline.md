# Baseline Observations

Record one entry per scenario run. Baseline is a fresh agent without the skill. Treatment is the same prompt with the skill loaded.

| Scenario | Date | Baseline behavior (verbatim rationalizations) | Treatment behavior | Verdict |
|---|---|---|---|---|
| S1 anti-slop dashboard (DepotDesk fixture, single static index.html, no question tool, no Skill calls) | 2026-09-13 | Competent but assembled from defaults: a four-counter stat row across the top ("They double as filters for the queue"), every region boxed in a rounded card (blocking list, work queue, bays, parts, chart), pill status badges with radius 999, red circular rank badges, a stacked bar chart added "by system" without a decision behind it, main-plus-rail card layout. Good instincts: top bar instead of sidebar ("a sidebar would cost 200+px of table width"), tabular mono numerals, amber single accent. No written brief, no directions, no DESIGN.md, no rubric; verified only that it compiled and rendered. | Ran all eleven stages. Wrote .design/brief.md (8 observed, 2 provided, 6 inferred), .design/brand-spec.md (Branch B), .design/directions.md with three structurally different directions scored on seven criteria and a synthesis pick, DESIGN.md with all ten sections plus an evidence table, .design/review.md with a two-round rubric (round 1 found two structural defects at 768 and fixed structure first), seven screenshots at 390, 768, 1440 via headless Chrome after Playwright was unavailable. Output: no stat row, no cards around content, no chart, grouped ranked list with hairlines, a product signature (the pull-out line through the list, mirrored as a proportional bar) that passes the brand-removal test. Self-score 21/25. Defaults report listed ten inferred decisions with rules. Reported honestly that axe and Lighthouse were not run. | Differentiated. The control showed the milder slop signals (stat row, card grid, decorative chart, pill overload) and the treatment removed all four and produced every required artifact. Caveat: the rubric score is a self-assessment; an independent judge run is still needed. |
| S12 finish and identity commitment (same DepotDesk fixture, skill v4.2.0 run through the installed frontend-director agent, then a blind judge) | 2026-09-14 | Treatment only; S1 baseline row is the control. | DESIGN.md states all six identity commitments with values: uppercase condensed wordmark plus depot code, Barlow Condensed / Barlow / mono pairing with fallbacks, warm paper temperature in oklch, one blue accent capped at three per viewport, 2 px radius with 1 px borders and no elevation, functional motion at 120/200 ms. Craft P0 all pass; loading, empty, single-row, and picker states rendered; 70-row deterministic seed data; check-tokens PASS after fixing three overflow causes and two contrast failures found by rendering. Self-score 25/30. Blind judge (screenshots, brief, rubric only): product fit 5, hierarchy 4, typography 4, rhythm 4, identity 4, finish 3, total 24/30. Judge named tablet defects the implementer missed: DUE column collides with the action button at 768, PARTS column vanishes silently, the 390 queue becomes 250 px key/value stacks, no menu affordance for the hidden nav. | Verdict REFINE, not PASS: the lower Finish score stands. The identity commitment and craft checklist visibly raised the output over the v4.0 run (compare docs/evals/s1-with-skill.png with docs/evals/s12-with-skill-v4.2.png), and the judge caught real defects self-scoring passed. Judge disagreement on Finish was 1 point, under the 2-point rule, so the judge's rationale was recorded and the lower score used. |
